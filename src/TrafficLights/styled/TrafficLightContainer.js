import styled from "styled-components";

const TrafficLightContainer = styled.div`
  width: fit-content;
  display: grid;
  grid-column-gap: 8px;
  grid-auto-flow: column;
`;

export { TrafficLightContainer };
