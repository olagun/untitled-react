/* 

state = {
  layers: [
    Symbol(): {type, x,y, width, height}
  ],
  cursors: {
    person: {x: useMotionValue(0),y: useMotionValue(0),active,hasTool}
  }
}

*/

function moveTo() {}

function click() {}

function chooseTool() {}


async function changeMotionValue(motionValue, value) {

}

function addText(person, text, x, y, width, height, fontSize) {
  const layerSymbol = Symbol("layer_symbol");
  


  // moveTo tool
  // clickTool
  // add text icon
  // moveTo x y
  // set active
  // start shape
  //      moveTo x + width, y + height
  //      moveShapeTo x + width, y + height
  // set inactive
  // moveTo x y - line height
  // remove text icon
  // add text cursor
  // until finish text, add new text at random intervals
  // remove text cursor
}

function drawImage() {}

function drawShape() {}
