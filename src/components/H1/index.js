import styled from "styled-components";
import { motion } from "framer-motion";

const H1 = styled.div`
  font-size: 3vw;
  font-weight: bold;
  line-height: 56px;
  margin: 0;
  margin-bottom: 16px;
  width: fit-content;
`;

export { H1 };
