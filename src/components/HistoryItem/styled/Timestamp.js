import styled from "styled-components";

const Timestamp = styled.div`
  font-size: 14px;
  color: rgba(0, 0, 0, 0.33);
  letter-spacing: 0;
  line-height: 14px;

  margin-top: 4px;
`;

export { Timestamp };
