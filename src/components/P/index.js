import styled from "styled-components";
import { motion } from "framer-motion";

const P = styled(motion.div)`
  font-size: 16px;
  line-height: 24px;
  margin: 0;
  margin-bottom: 16px;
  max-width: 60%;
  width: fit-content;
`;

export { P };