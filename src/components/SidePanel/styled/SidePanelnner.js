import styled from "styled-components";
import { motion } from "framer-motion";

const SidePanelInner = styled(motion.div)`
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
`;

export { SidePanelInner };
